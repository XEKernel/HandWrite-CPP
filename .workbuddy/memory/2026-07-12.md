# 2026-07-12 工作日志

## 全面代码审查及修复

### P0 修复（崩溃风险）
- **MainWindow 析构 use-after-free**: `closeEvent` 和析构函数中 `waitForFinished()` 后添加 `QCoreApplication::processEvents()` 清空排队的跨线程 `QMetaObject::invokeMethod` 事件
- **字体路径索引不匹配**: 添加 `m_cachedFontNames`/`m_cachedFontPaths` 成员缓存 `getTtfFiles()` 结果；修复 `tools.cpp` 中 `getTtfFiles()` 重复扫描问题

### P1 修复（功能 Bug）
- **纸张模板选择器索引错乱**: 添加 `getPaperSizeByDisplayName()` 方法，`onComboBoxPaperTemplateCurrentIndexChanged` 改用显示名称查找；`populateComboBoxes` 用 `findText("默认")` 设置正确默认项
- **字符覆盖在非并行路径不生效**: 重写 `generatePreview` 使用 `layoutPages` + `renderPageStatic`，统一所有渲染路径
- **扰动后字符间距测量**: `drawTextWithPerturbationStatic` 添加 `QFontMetrics perturbedFm(perturbedFont)` 计算正确的前进距离
- **行间距布局/渲染不一致**: 添加 `PageRenderData::lineYPositions` 预计算 Y 坐标，`renderPageStatic` 直接使用不再二次随机

### P2 修复（代码质量）
- **代码重复消除**: 提取公共 `layoutPages()` 方法，`generatePreview`、`generatePreviewParallel`、`generateImageParallel` 共用
- **未使用变量/头文件**: 移除 `contentHeight`、`#include <filesystem>`（config.cpp），`config.hpp` 冗余头文件移至 .cpp
- **移除死代码**: 删除不再使用的非静态 `renderPage()` 和 `drawTextWithPerturbation()`

### P3 修复（健壮性）
- **输入验证**: 为所有 `QLineEdit` 添加 `QIntValidator`/`QDoubleValidator`
- **配置解析健壮性**: 数组解析添加空元素检查和 try/catch

### 简化析构函数
- 移除析构函数中与 `Qt` parent-child 机制冲突的手动 delete（`m_previewWatcher`/`m_exportWatcher` 由 Qt 管理）
- `closeEvent` 和析构函数的清理逻辑去重

### 编译验证
- 尝试 GCC (MSYS2 mingw64) 编译但因 ucrt64/mingw64 环境 DLL 冲突失败
- 项目路径含中文导致 MinGW 编码问题（已知限制）
- VS 2026 预览版 MSBuild 崩溃（环境问题）
- 代码修改通过手动审查确认正确，建议在纯净 mingw64 shell 或修复 VS 后编译验证
- 如需在 ucrt64 编译：`pacman -S mingw-w64-ucrt-x86_64-qt6-base` 安装 ucrt64 版 Qt6

## 14项新功能实现

### 核心引擎扩展 (core.hpp/cpp)
1. **纸张纹理**: PaperTexture 枚举 (None/HorizontalLine/Grid/TianZiGe/Composition/DotGrid)，drawPaperTexture() 绘制
2. **笔触效果**: strokeWidthSigma 笔画粗细扰动、applyInkBleed() 墨水洇染后处理、strikeThroughRate 涂改模拟
3. **段落排版**: paragraphIndent 段首缩进两字符、paragraphSpacing 段间距、layoutText 重构支持
4. **Markdown 解析**: parseMarkdown() 支持 # 标题、~~删除线~~、--- 分割线
5. **字体混合**: pickMixedFont() 随机切换到 fontMixList 中的字体(20%概率)
6. **背景图片叠加**: backgroundImagePath 支持

### 数据结构
- TemplateParams 新增 15 个字段覆盖所有新功能
- PageRenderData 新增 lineParagraphIndex
- StyledSpan 结构体用于 Markdown 解析

### UI (mainwindow.hpp/cpp)
7. **实时自动预览**: QTimer debounce 500ms，参数变化自动刷新
8. **PDF 导出**: exportPdf() + UI 按钮，使用 QPrinter
9. **多预设管理**: presetDir()/refreshPresetList()，QListWidget + 保存/加载/删除按钮
10. **动态 UI**: setupDynamicUi() 程序化创建纹理、排版、效果控件
11. **背景图片选择**: 选择/清除按钮 + 路径标签

### CLI (cli.cpp)
12. **命令行模式**: handwrite-cli，支持 --input/--text/--output/--preset/--rate/--format
13. 支持 stdin 管道输入和文件输入
14. 支持 PNG 和 PDF 两种输出格式

### 配置扩展 (config.hpp/cpp)
- 新增 15 个配置字段的 getter/setter
- 支持 bool 类型 (true/false 序列化)

### CMakeLists.txt
- 新增 handwrite-cli 可执行目标
- 项目版本升至 2.0.0

### 编译验证通过 (12:06)
- 用户重装 MSYS2，项目移至 G:\build（无中文路径）
- 安装 GCC 16.1.0 + Qt6 6.11.1 + CMake 4.4.0 + Ninja (ucrt64)
- 配置清华/中科大/北外镜像加速
- 修复 2 个编译错误：QChar+QChar 歧义、decltype 引用类型
- HandWrite.exe (3.9MB GUI) + handwrite-cli.exe (3.6MB CLI) 编译成功

### 运行时问题排查与修复 (12:34)
- **根本问题**: MSYS2 bash 环境下的 ucrt64 DLL 与程序 DLL 冲突导致 crash/hang
- **QCoreApplication→QGuiApplication**: CLI 使用 QFontDatabase 必须用 QGuiApplication
- **解决方案**: 从 Windows PowerShell/资源管理器直接运行，设置 QT_PLUGIN_PATH
- **验证通过**: CLI 成功生成 667x945 PNG；GUI 通过 Start-Process 正常启动
- 复制 qoffscreen.dll/qminimal.dll 到 platforms/ 目录

### 崩溃根因定位与修复 (12:59-13:08)
- **空指针崩溃**: populateComboBoxes 中 comboBoxPaperTemplate 信号触发 onParameterChanged → m_autoPreviewTimer(未初始化)→空指针崩溃
- **修复**: timer 提前到 setupConnections 后创建 + blockSignals(true) 阻止初始化信号
- **DLL 全量部署**: 从 ucrt64/bin 复制全部 117 个 DLL 解决资源管理器双击启动问题
- **右侧面板**: 从固定最大宽度 320px 改为 QSplitter 可拖拽分割条

### 纹理网格对齐 (18:47)
- 选择纹理时自动调整行间距为网格尺寸整数倍 (25px或28px)
- layoutPages 中文字基线吸附到最近网格线

### 背景图片透视校准 (18:52-19:15)
- 添加 CalibrationDialog: 四点锚点标记纸的四角
- QTransform::quadToQuad 透视变换将文字映射到歪斜纸张上
- **Bug**: 对话框X关闭返回Rejected, 改为确定按钮显式accept
- GitHub Actions: 推送main分支 + CI自动编译 + Release
