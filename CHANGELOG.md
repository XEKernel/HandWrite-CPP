# 更新日志

本文件是 GitHub Release 说明的**唯一来源**：打 tag 推送后，Actions 会截取
`## [<版本号>]` 到下一个 `## [` 之间的内容，作为该版本 Release 的正文，
并附上「自上一个 tag 以来的提交列表」。

**格式约定（CI 依赖，请勿改动）**：版本标题必须是 `## [x.y.z] - YYYY-MM-DD`，
小节用 `### 新增` / `### 修复` / `### 变更`。

版本号规则：`主版本.功能添加.小修复` —— 仅修 BUG → 第三位 +1；含新功能 → 第二位 +1。

---

## [2.9.0] - 2026-09-25

### 新增

- **横线导引 · 水平透视补偿** —— 斜拍时纸是梯形，同样多的字在窄的一侧应该占更窄的宽度。
  现在用锚点四角双线性插值算出每一行文字块在照片上的**实际左边界**与**实际宽度**，
  逐行做水平平移 + 缩放（`x → originX + (x - contentLeft) * scale`）。
  至此锚点与横线真正协同：

  - **锚点** 负责「文字块在哪、多宽、多斜」（页面级几何）
  - **横线** 负责「每一行走哪条曲线」（行级弯曲）

  正拍且锚点＝画布四角时缩放恰好为 1.0，**不改变任何既有行为**（已做正拍对照验证）。

- **横线导引 · 自动检测** —— 「横线...」对话框新增 **「自动检测」** 按钮，一键标定整页横线。
  纯手写、零外部依赖：

  - **三分类亮度**：横线的亮度介于纸面（亮）与背景（暗，如桌面）之间，统计每行
    "中间亮度像素占比" —— 横线行高、纸面与背景行都接近 0，天然免疫光照不均与深色背景
    （"找最暗行"的思路会被深色桌面带偏）
  - **等距规则化**：间距取相邻峰的中位数，被字迹/污渍遮挡而漏检的线会被补回来
  - **逐列跟踪**：以上一个采样点为搜索中心，让结果是**曲线**，能表达纸张弯曲；
    某列块找不到横线（纸外/遮挡）时保持当前 y，不会把曲线拽到相邻横线上
  - **自适应关键曲线**：默认只取首尾 2 条，中间弯曲非线性（鼓/凹）时自动补中间 1 条做分段插值
  - 检测结果是**候选**，用户可继续拖拽微调 / 增删；失败时明确提示改用手动描线
    （红色/深色横线、强烈阴影、文字过密都会失败，不硬猜）

### 修复（横线导引相关）

- **CLI 加载预设时背景锚点校准永远不生效**：`cal.enabled = cal.isValid()` 的顺序反了 ——
  `isValid()` 自身要求 `enabled` 为真，于是网格刚填好就被判定无效，锚点被静默丢弃。
  GUI 路径没有这个问题（它先设了 `enabled = true`）。现在改为先置真再校验，
  并在 `BackgroundCalibration::isValid()` 上加了显式注释以免再犯。

---

## [2.8.2] - 2026-09-25

### 新增

- **横线导引 · 手绘 UI** —— 在前面引擎的基础上补上界面，功能至此可用。

  - `ImageCanvasDialog` 基类：把「载入背景 + 等比缩放居中 + 坐标换算 + resize 重算」
    从 `CalibrationDialog` 抽出来共用，避免第三份重复实现（`CalibrationDialog` 已改为继承它）
  - `LineGuideDialog`：**沿照片上的横线按住鼠标拖动即可描线**；只需描第 1 条和最后 1 条，
    中间按条数自动插值（2 条关键曲线 → 整页 N 条）；关键曲线按**垂直位置**自动排序
    —— 插值在空间相邻的两条之间做，用户描线的先后顺序不能决定分段；采点按 ≥3px 抽稀 +
    松手后移动平均平滑；端点可拖拽微调、整条可整体平移；**3× 放大镜**
    （弯曲的线在缩略图上看不真切，这是能画准的关键）；撤销栈；`Delete` 删除选中；`Esc` 取消描线
  - 参数面板：条数、关键曲线间是否自动插值、基线位置滑块（0~1）+ 像素微调、
    逐字跟随弯曲、每行占几条线
  - 主窗口「背景图片」分组新增 **「横线...」** 按钮；锚点未标定时给出提示但不强制
    （正拍照片不需要锚点也能描线）

### 修复

- 清除背景图时一并清除横线导引（导引存的是原图坐标，背景没了就没有意义）。

---

## [2.8.1] - 2026-09-25

### 新增

- **横线导引 · 渲染引擎** —— 让文字排布在作业本照片的印刷横线内。
  纸张会弯曲，所以横线是**曲线**，用采样折线表示；文字**逐字**沿曲线定位（y 偏移 + 切线角叠加），
  不再是"整行平移"。配合背景图使用时**跳过 `warpMesh`**（导引曲线本身已含透视与弯曲的全部形变，
  再 warp 一圈只会被 3×3 网格的分片近似拖出误差）。

  - `GuideCurve`：`sample()`（线性插值 + 端点夹取 + 切线角）、`normalize()`（排序去重 + 移动平均平滑）、
    `resampleByArcLength()`
  - `LineGuideSet`：**手绘 2 条关键曲线 + 填条数 → 中间按弧长参数插值**，把 20+ 条的绘制成本降到 2~3 条；
    支持多关键曲线分段插值、`linesPerRow`（一行占几条线）、`baselineRatio`（基线位置）、
    `followCurve`（是否跟随弯曲）
  - 分页：横线用尽自动翻页，第二页重新从第一条横线开始（作业本每页排版相同）
  - 配置持久化 8 个 `line_guide_*` 键；GUI 预设往返不丢数据；CLI `-p` 预设可完整读取

### 修复

- `Config` 的 double 数组若值恰好全是整数，会被存成 int 数组导致按 double 读回时整组丢失
  —— 锚点、导引曲线这类恰好落在整数像素上的坐标会中招。现在 `getDoubleArray` 统一兼容两种存储。
- `alignToGrid` 与横线导引同时启用会得到无意义位置（导引启用时强制关闭纹理网格吸附）；
  有背景照片时不再把程序纹理画在照片上。

---

## [2.7.0] - 2026-09-12

针对 `docs/CODE_REVIEW_2026-09-12.md` 的完整静态审查结果，修复全部 P0/P1/P2 问题，
并补上审查中建议的高价值功能。

### 新增

- **SVG 多页导出**（GUI「导出 → 导出 SVG」、CLI `-f svg`）。原实现只导第 1 页，
  多页文档会静默丢内容；现在首页写 `output.svg`，其余按 `output-2.svg`、`output-3.svg` 递增。
- **随机种子复现**：新增 `seed` 参数、GUI「种子」输入框与「随机」按钮、CLI `-s/--seed`。
  同一种子下预览与导出的结果**逐字节一致**，可直接复现某张满意的抖动效果。
- **混合字体真正可用**：`fontMixList` + `fontMixRate` 之前是彻底无法启用的死功能
  （无任何赋值点）。现在 GUI 提供「混合字体」选择对话框与出现概率，随机切换时可复现。
- **中文标点保留开关**：新增 `preserveChinesePunctuation`（GUI 复选框，默认**保留**）。
  此前中文标点会被无条件替换成 ASCII，且用户无从关闭。
- **文字变形强度可调**：新增 `textWarpStrength`（GUI 数值框 + 预设持久化）。
- **逐字文字变形**：变形量改为按字符位置累积相位，替代原来的「整行整体平移」，
  弧形/波浪/环形模板现在是真的在弯折每个字。
- **中文避头尾（禁则处理）**：标点不再孤悬于行首，行尾起始标点会被挪到下一行。
- **进度对话框可取消**：接上取消回调与取消标志，「取消」按钮真正生效。
- **CLI 参数补全**：新增 `-f svg`、`-s/--seed`；`-p` 预设现在完整读取（纸张纹理、透明度、
  背景图、混合字体、排版方向、文字变形、标点策略、背景锚点校准、字符级覆盖、种子），
  与 GUI 保存的字段一一对应。
- **单元测试**：新增 `handwrite-tests` 目标（88 项断言，覆盖标点转换、Markdown 解析、
  字符覆盖序列化、文本排版、配置往返、内存预算、字体检查），`ctest` 可直接跑。
- **代码规范**：新增 `.clang-format`（沿用现有写法，非大范围重排）；
  三个目标全部启用 `-Wall -Wextra` 并做到**零告警**。

### 修复

严重（P0）：

- **数据安全回归**：GUI 导出不再清空 `outputs/` 目录下的全部文件，
  改为只清理本程序生成的「纯数字命名 PNG」，避免误删用户放在该目录的资料。
- **高倍率必然 OOM 崩溃**：x32/x64 会直接耗尽内存。新增单页/峰值内存预算检查，
  超限时抛出可读原因（并提示降低倍率或关闭洇染/背景图），而不是把进程撑死；
  导出路径改为「逐页渲染并立即落盘」，峰值 = 线程数 × 单页而非页数 × 单页。
- **工作线程访问 UI 控件（未定义行为）**：导出与 PDF 的文本改为在主线程先行取出后再进线程。
- **每页重复解码背景图**：改为整篇只解码缩放一次，并跨页共享（QImage 隐式共享零拷贝）。

功能缺陷（P1）：

- **预设里所有小数被静默截断成整数**：`std::stoi("0.35")` 只解析前导 `0` 就返回且不报错，
  导致 `font_mix_rate`、`texture_opacity`、`perturb_theta_sigma` 等**全部小数参数**
  保存后再加载都变成 0。现在要求数值必须「整串被消费」才算合法数字。
- **预设保存/加载不完整**：排版方向、文字变形、标点策略、混合字体、种子、
  背景锚点校准、字符级覆盖等参数此前保存后即丢失。
- **Markdown 删除线不闭合**：`~~废弃~~保留` 中的「保留」被误加删除线。
- **`layoutText` 内存泄漏 + 字号度量串用**：每个带字号覆盖的 span 都 `new` 一个
  `QFontMetrics` 且不复位指针，既泄漏又让标题字号串到正文导致提前换行。
- **字符级覆盖索引错位**：改用「渲染索引 → 原文索引」映射，缩进等程序插入字符标记为 -1。
- **配置混合数组丢数据**：`[1, 2.5]` 这类数组会丢掉已解析的整数项。
- **配置字符串被切碎**：含逗号或引号的字符串（如字符覆盖 CSV、含逗号的字体路径）现在正确转义
  并以「引号感知」的方式切分。
- **字体加载失败静默回退**：新增 `checkFontAvailable`，字体不可用时明确报错并指出路径，
  不再静默改用系统默认字体让用户困惑。
- **「关于」对话框图标空白**：原路径 `:/resources/app.ico` 因仓库无任何 `.qrc` 必定落空；
  改为多级回退查找，找不到时优雅降级而非空白。
- **背景校准对话框高 DPI 重叠**：全部 `setGeometry` 绝对定位改为真实 layout，
  并随窗口尺寸重算画布；写死的白色文字改为跟随主题。
- **预设目录不可写**：从 `applicationDirPath()/presets`（装在 `Program Files` 下会写不进）
  改为 `QStandardPaths::AppDataLocation`。
- **进度对话框无法取消**：见「新增」。

整洁性（P2）：

- 删除死代码：`generateImage`、`findUnsupportedChars`、`setPaperSize`、`setMargins`、
  `setSpacing`、`setColors`、`setPerturbations`、`setRate`、只写不读的 `m_exportRate`。
- 忙等待轮询 `while (!future.isFinished()) { msleep(50); ... }` → `QFutureWatcher`。
- 修改**全局** `QThreadPool` → 改用具局部 `QThreadPool` 实例，不再波及别的 `QtConcurrent` 使用者。
- 竖排旋转 + 居中裁切逻辑重复两份 → 提取为 `rotateToVertical`。
- `mainwindow.hpp` 重复的 `#include <QDialog>` 已去重。
- 预设扩展名统一为 `.conf`（旧的 `.toml` 仍可读、仍会列出）。
- CLI 的「文本长度」按字符统计（原先按字节，中文会虚高约 3 倍）。

---

## [2.6.1] - 2026-07-29

### 新增

- 背景图片锚点校准双模式（整页网格 / 局部区域），修复模式切换时锚点未复位的问题。

### 变更

- 产品网页迁移到 `XEKernel.github.io`，仓库内 `Web/` 目录移除，改由仓库推送自动同步。

---

## [2.5.1] - 2026-07-24

### 修复

- 依据代码审查修复安全性、正确性、性能与构建问题。

---

## [2.5.0] - 2026-07-19

### 新增

- 撤销/重做、文件拖放、CLI 批处理模式、文字变形模板、SVG 导出。

### 修复

- 若干 BUG 修复与死代码清理。

---

[2.7.0]: https://github.com/XEKernel/HandWrite-CPP/compare/v2.6.1...v2.7.0
